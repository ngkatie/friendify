<div class="container">    
    <label for="timeframe">Select Timeframe:</label>
    <select id="timeframe" name="timeframe">
        <option value="4weeks">Last Month</option>
        <option value="6months">Last 6 Months</option>
        <option value="alltime">All-Time</option>
    </select>
    <div class="spotify-data">
        <button type="button" href="./userProfile.handlebars">Back to Profile</button>
        <button type="button" href="./top-songs.handlebars">Top 50 Songs</button>
        <button type="button" href="./daily-playlist.handlebars">Daily Playlist</button>
    </div>
    <div class="results">
        <h2>Top 50 Artists</h2>
        <div class="results-container">
            {{#each topArtists}}
                <div class="artist-item">
                    <div class="artist-image">
                    <img src="{{image}}" alt="Artist Image" class="img-thumbnail" width="200" height="200">
                    </div>
                    <div class="artist-info">
                        <h3>{{artistName}}</h3>
                    </div>
                </div>
            {{/each}}
        </div>
    </div>
</div>